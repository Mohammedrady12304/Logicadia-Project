import { XpToLevelPipe } from './xp-to-level-pipe';

describe('XpToLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new XpToLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
